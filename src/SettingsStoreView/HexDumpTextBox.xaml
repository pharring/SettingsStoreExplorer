<UserControl x:Class="SettingsStoreView.HexDumpTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SettingsStoreView"
    >
    <UserControl.Resources>
        <local:BinaryToHexDumpConverter x:Key="binaryToHexDumpConverter"/>
    </UserControl.Resources>
    <TextBox Text="{Binding Mode=OneWay, Converter={StaticResource binaryToHexDumpConverter}}" PreviewTextInput="OnPreviewTextInput" SelectionChanged="OnSelectionChanged" />
</UserControl>
